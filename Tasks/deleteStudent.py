def deleteStudent(student_number):
    """
    Deletes a student record from a text file located at /Student_Records.

    This function searches the specified text file for a record matching the given student number. If a match is found, the record is deleted, and details of the deleted student are returned in a dictionary.

    Parameters:
        student_number (str): The unique identifier for the student.

    Returns:
        dict: A dictionary containing details of the deleted student, including:
            - 'student_name': Name of the student.
            - 'student_number': The student's unique identifier.
            - 'course_name': The student's course name.

    Note:
        This function requires the implementation of error handling.
    """
    pass